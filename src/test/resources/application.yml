server:
  port: 8080
  servlet:
    encoding:
      charset: utf-8
      enabled: true

spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB



logging.level:
  org.hibernate.SQL: trace
